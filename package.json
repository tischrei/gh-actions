{
  "name": "gh-actions",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "yarn run build && touch ./out/.nojekyll && gh-pages -d out -t true",
    "serve": "npx serve out"
  },
  "dependencies": {
    "gh-pages": "^6.1.1",
    "next": "14.2.2",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "eslint": "^8",
    "eslint-config-next": "14.2.2"
  }
}
